test:
  service: Disk
  root: <%= Rails.root.join("tmp/storage") %>

local:
  service: Disk
  root: <%= Rails.root.join("storage") %>


google:
  service: GCS
  iam: true
  project: "playground-355706"
  bucket: "playground-blog"


